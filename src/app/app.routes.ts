import { Routes } from '@angular/router';
import { FormComponent } from './form/form.component';

export const routes: Routes = [
  { path: '', component: FormComponent }, // главная страница — наша форма
  // другие маршруты, если есть
];